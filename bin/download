#!/usr/bin/env bash
set -euo pipefail

echo "Cloning Watchman v$ASDF_INSTALL_VERSION into $ASDF_DOWNLOAD_PATH..."
git clone --depth=1 --branch "v$ASDF_INSTALL_VERSION" https://github.com/facebook/watchman.git "$ASDF_DOWNLOAD_PATH"
